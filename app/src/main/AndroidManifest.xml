<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhanglian.collect">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Startup">
        </activity>
        <activity android:name=".ui.home.HomeAdd" />
        <activity android:name=".ui.home.PeopleAdd" />
        <activity android:name=".ui.home.PeopleList" />
        <activity android:name=".ui.home.HomeDetails" />
        <activity android:name=".ui.news.NewsList" />
        <activity
            android:name=".ui.home.HomeList"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>